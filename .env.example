### Copy this file to `.env` and fill in real credentials. NEVER commit `.env` to version control.
DB_HOST=dionysus.hostns.io
DB_USER=u80655_schoolmngt
DB_PASSWORD=Bernice@2026
DB_NAME=u80655_schoolmngt
DB_PORT=3306

# Optional: Flask secret key for session signing
# SECRET_KEY=change_me_to_a_random_value
